<%= simple_form_for ([@list, @bookmark]) do |f| %>
  <%= f.input :comment %>
  <%= f.input :movie, collection: Movie.all %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Homepage', lists_path %> |
